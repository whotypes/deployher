# Committed dummy values for CI (GitHub Actions) and local `bun test` without secrets.
# Copy to .env before tests: cp test/ci.env .env
APP_ENV=development
HOSTNAME=127.0.0.1
GITHUB_CLIENT_ID=ci-test-github-client-id
GITHUB_CLIENT_SECRET=ci-test-github-client-secret
BETTER_AUTH_SECRET=ci-test-better-auth-secret
RUNNER_SHARED_SECRET=ci-test-runner-shared-secret
